# Origin of the RIDDANCE API for production builds, e.g.
#   VITE_API_URL=https://riddance-api.onrender.com
# Leave unset for local development — Vite proxies /api to http://localhost:5000.
VITE_API_URL=
